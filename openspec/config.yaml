# SDD Project Configuration — control-cuentas-web
# Auto-generated by sdd-init on 2026-07-12

project:
  name: control-cuentas-web
  description: Aplicación de control de gastos e ingresos personales con dashboard, charts y categorías.
  stack:
    framework: Next.js 16.2.9 (App Router)
    language: TypeScript 5
    database: PostgreSQL via Prisma 7
    auth: NextAuth v5 beta (credentials + JWT)
    ui: shadcn/ui + Tailwind CSS v4 + Base UI
    charts: Recharts 3
    animations: framer-motion 12
    forms: react-hook-form + zod 4
    theme: next-themes

testing:
  strict_tdd: false
  mode: standard
  runner: vitest
  runner_version: latest
  test_command: npx vitest run
  test_watch_command: npx vitest
  coverage_provider: v8
  component_testing: "@testing-library/react"
  integration_testing: vitest + supertest (or fetch-based)
  database_testing: "Prisma with test database or SQLite in-memory"

phase_rules:
  sdd_explore:
    description: "Read-only exploration of existing code; no files created"
  sdd_proposal:
    description: "Architectural decisions and approach"
  sdd_spec:
    description: "Structured acceptance criteria"
  sdd_design:
    description: "Architecture and module design"
  sdd_tasks:
    description: "Mechanical task breakdown"
  sdd_apply:
    description: "Implementation with standard mode (no strict TDD)"
  sdd_verify:
    description: "Validation against spec"

conventions:
  language: TypeScript
  formatting: ESLint (eslint-config-next)
  imports: "@/" path alias for src/
  component_pattern: "Feature-based folders in src/components/"
  api_pattern: "App Router route handlers in src/app/api/"
  auth_guard: "auth() from @/lib/auth on every endpoint"
